#ifndef UTIL_H
#define UTIL_H

namespace util{
    
    double doubleModulo(double a, double b);
    
    int doubleRound(double a);

    double radianToDeg(double rad);
    
    double degToRadian(double deg);
}

#endif